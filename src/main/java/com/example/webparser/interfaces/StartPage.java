package com.example.webparser.interfaces;

import org.openqa.selenium.WebDriver;

public interface StartPage {

    void openStartPage(WebDriver webDriver);
    void startAuthentication(WebDriver webDriver);
}
